%Name: Jacob Smith
%Date: November 5 2018
%Assignment: ENGR1125 Problem Set 9, using complex analysis on circuits
%resistor values in ohms

%setup variables used in all problems
%resistors values in ohms
R1=1*10^3;
R2=10*10^3;
%capcitor values in farads
C1=1*10^-6;
C2=.1*10^-6;
%angular frequencies
w=logspace(-3,3);

%problem 1
vOut=1./(-j*w*C1*R1);
genBode('Problem 1',w,abs(vOut),angle(vOut);
%problem 2
vOut=-R2./(-j*w*C1*R1);
genBode('Problem 2',w,abs(vOut),angle(vOut);


